<nav class="sb-topnav navbar navbar-expand navbar-dark bg-dark">
    <!-- Navbar Brand -->
    <a class="navbar-brand ps-3" href="@Url.Action("Index", "Dashboard")">WebSIMS</a>
    
    <!-- Home Link -->
    <a class="nav-link" href="@Url.Action("Index", "Dashboard")">Home</a>

    <!-- Sidebar Toggle -->
    <button class="btn btn-link btn-sm order-1 order-lg-0 me-4 me-lg-0" id="sidebarToggle">
        <i class="fas fa-bars"></i>
    </button>

    <!-- Navbar Search - Moved closer to the left -->
    <form class="d-none d-md-inline-block ms-4 my-2" style="width: 300px;" method="get" asp-controller="Course" asp-action="Search">
        <div class="input-group">
            <input class="form-control" name="query" type="text" placeholder="Search for courses..." aria-label="Search">
            <button class="btn btn-primary" type="submit">
                <i class="fas fa-search"></i>
            </button>
        </div>
    </form>

    <!-- Spacer to push user menu to the right -->
    <div class="flex-grow-1"></div>

    <!-- Navbar User Dropdown -->
    <ul class="navbar-nav ms-auto me-3">
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" id="navbarDropdown" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                <i class="fas fa-user fa-fw"></i>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                <li><hr class="dropdown-divider" /></li>
                <li>
                    <form method="post" action="@Url.Action("Logout", "Authen")">
                        <button class="dropdown-item" type="submit">Logout</button>
                    </form>
                </li>
            </ul>
        </li>
    </ul>
</nav>
